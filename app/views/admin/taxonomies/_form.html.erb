<% options = options_from_collection_for_select(Taxonomy.roots, :id, :name, @taxonomy.parent_id) %>
<%= f.select_control @taxonomy, :parent_id, {}, { :include_blank => '', :options => options } %>

<%= f.text_field_control @taxonomy, :name %>

<%= f.text_field_control @taxonomy, :code %>